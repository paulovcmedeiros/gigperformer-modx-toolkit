Include "gigperformer-modx-toolkit/gpscript/GeneralUtils/MainAndChannelOnOffSwitches"

On MidiEvent(m : MidiMessage)
    Var
        isPB : Boolean = IsPitchBend(m)

    If (isPB And MODXTK_ChannelIsAllowed(GetChannel(m))) Or Not isPB Then
        SendNow(m)
    End
End
